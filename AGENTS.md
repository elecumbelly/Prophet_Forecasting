# Notes

- Latest push to `origin/main` includes shadcn/Next.js frontend rebuild and Flask JSON API endpoints.
- Frontend dev server runs on port 3001 (`frontend/package.json` dev script).
- Backend Flask API runs on port 5001.
- UI should show a `LYRA-V2` badge in the header to confirm the latest build is loaded.
